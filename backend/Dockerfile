FROM node:alpine

ARG DB_HOST
ARG DB_USER
ARG DB_PASSWORD
ARG DB_NAME
ARG DB_PORT

ENV DB_HOST=$DB_HOST \
    DB_USER=$DB_USER \
    DB_PASSWORD=$DB_PASSWORD \
    DB_NAME=$DB_NAME \
    DB_PORT=$DB_PORT

WORKDIR /app

COPY . .

RUN npm install

EXPOSE 3001

CMD [ "npm", "start" ]